name: Build, sign and release Stellio

on:
  push:
    branches: [main]
    paths:
      - 'main.py'
      - 'index.html'
      - 'script.js'
      - 'style.css'
      - 'launcher.py'
      - 'check_deps.py'
      - 'requirements.txt'
      - 'stellio-setup.iss'
      - 'assets/**'
      - 'languages/**'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Build uniquement, sans publier de release'
        type: boolean
        default: false

permissions:
  contents: write  

jobs:
  # ──────────────────────────────────────────────────────────────────────
  # 1. Détecte la version courante et decide si une release est necessaire
  # ──────────────────────────────────────────────────────────────────────
  check-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.check.outputs.version }}
      should_release: ${{ steps.check.outputs.should_release }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Extract version and compare to existing tags
        id: check
        run: |
          VERSION=$(grep -E '^CURRENT_VERSION' main.py | head -1 | sed -E "s/^CURRENT_VERSION[[:space:]]*=[[:space:]]*//; s/[\"']//g" | tr -d '[:space:]')
          if [ -z "$VERSION" ]; then
            echo "CURRENT_VERSION introuvable dans main.py"
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Version detectee : $VERSION"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.dry_run }}" = "true" ]; then
            echo "should_release=false" >> "$GITHUB_OUTPUT"
            echo "Mode dry_run : build uniquement, pas de release."
          elif git ls-remote --tags origin | grep -q "refs/tags/v$VERSION\$"; then
            echo "should_release=false" >> "$GITHUB_OUTPUT"
            echo "Le tag v$VERSION existe deja — rien a publier (CURRENT_VERSION pas modifiee)."
          else
            echo "should_release=true" >> "$GITHUB_OUTPUT"
            echo "Nouvelle version : v$VERSION va etre construite et publiee."
          fi

  # ──────────────────────────────────────────────────────────────────────
  # 2. Build (tourne toujours, meme en dry_run, pour valider le code)
  # ──────────────────────────────────────────────────────────────────────
  build:
    needs: check-version
    runs-on: windows-latest
    outputs:
      artifact-id: ${{ steps.upload-unsigned.outputs.artifact-id }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m venv venv
          venv\Scripts\python.exe -m pip install --upgrade pip
          venv\Scripts\python.exe -m pip install --upgrade -r requirements.txt
          venv\Scripts\python.exe -m pip install pyinstaller

      - name: Check dependencies (fix)
        run: venv\Scripts\python.exe check_deps.py --fix

      - name: Final dependency check (no fix)
        run: venv\Scripts\python.exe check_deps.py

      - name: Build with PyInstaller
        run: |
          venv\Scripts\python.exe -m PyInstaller `
            --name "Stellio" `
            --onedir `
            --windowed `
            --noconfirm `
            --clean `
            --hidden-import sqlite3 `
            --hidden-import ssl `
            --hidden-import hashlib `
            --hidden-import tkinter `
            --hidden-import tkinter.ttk `
            --hidden-import nest_asyncio `
            --collect-all tkinter `
            --collect-all webview `
            --collect-all trimesh `
            --collect-all pyrender `
            --collect-all imageio `
            --collect-all OpenGL `
            --collect-all numpy `
            --collect-all PIL `
            --collect-all matplotlib `
            --collect-all mpl_toolkits `
            --collect-all fast_simplification `
            --collect-all pymeshfix `
            --collect-all shapely `
            --collect-all rectpack `
            --collect-all smbprotocol `
            --collect-all smbclient `
            --collect-all paho `
            --collect-all cryptography `
            --collect-all rarfile `
            --collect-all py7zr `
            --collect-all defusedxml `
            --collect-all qrcode `
            --collect-all websocket `
            --collect-all flashforge `
            --collect-all waitress `
            --collect-all flask `
            --collect-all werkzeug `
            --copy-metadata imageio `
            --copy-metadata pyrender `
            --copy-metadata trimesh `
            --copy-metadata Pillow `
            --copy-metadata PyOpenGL `
            --copy-metadata numpy `
            --icon="assets\logo-nom-stellio.ico" "launcher.py"

      - name: Verify PyInstaller output exists
        shell: pwsh
        run: |
          if (-not (Test-Path "dist\Stellio\Stellio.exe")) {
            throw "dist\Stellio\Stellio.exe introuvable - la compilation a echoue"
          }

      - name: Copy patchable files into dist\Stellio\app
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "dist\Stellio\app" | Out-Null
          Copy-Item "main.py"          "dist\Stellio\app\main.py"
          Copy-Item "check_deps.py"    "dist\Stellio\app\check_deps.py"
          Copy-Item "index.html"       "dist\Stellio\app\index.html"
          Copy-Item "script.js"        "dist\Stellio\app\script.js"
          Copy-Item "style.css"        "dist\Stellio\app\style.css"
          Copy-Item -Recurse "assets"    "dist\Stellio\app\assets"
          Copy-Item -Recurse "languages" "dist\Stellio\app\languages"
          if (Test-Path "bin") { Copy-Item -Recurse "bin" "dist\Stellio\app\bin" }

          foreach ($f in @("main.py","check_deps.py","index.html","script.js","style.css")) {
            if (-not (Test-Path "dist\Stellio\app\$f")) {
              throw "Echec de copie : dist\Stellio\app\$f"
            }
          }

      - name: Create patch ZIP for auto-updater
        shell: pwsh
        run: |
          $zipName = "Stellio-v${{ needs.check-version.outputs.version }}-patch.zip"
          Compress-Archive -Path "dist\Stellio\app\*" -DestinationPath $zipName -Force
          if (-not (Test-Path $zipName)) { throw "Echec de creation du ZIP de patch." }
          $hash = (Get-FileHash -Algorithm SHA256 $zipName).Hash.ToLower()
          "$hash  $zipName" | Out-File -Encoding ascii "$zipName.sha256"

      - name: Upload patch ZIP artifact
        uses: actions/upload-artifact@v4
        with:
          name: stellio-patch-zip
          path: |
            Stellio-v${{ needs.check-version.outputs.version }}-patch.zip
            Stellio-v${{ needs.check-version.outputs.version }}-patch.zip.sha256

      - name: Install Inno Setup
        run: choco install innosetup -y

      - name: Compile installer
        shell: pwsh
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" `
            "/DMyAppVersion=${{ needs.check-version.outputs.version }}" `
            "stellio-setup.iss"

      - name: Upload unsigned installer artifact
        id: upload-unsigned
        uses: actions/upload-artifact@v4
        with:
          name: stellio-installer-unsigned
          path: output\Stellio-Setup-${{ needs.check-version.outputs.version }}.exe

  # ──────────────────────────────────────────────────────────────────────
  # 2bis. Build macOS (.app + .dmg)
  # ──────────────────────────────────────────────────────────────────────
  build-macos:
    needs: check-version
    strategy:
      matrix:
        include:
          - runner: macos-latest
            arch: arm64
          - runner: macos-15-intel
            arch: x86_64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python3 -m venv venv
          venv/bin/python -m pip install --upgrade pip
          venv/bin/python -m pip install --upgrade -r requirements.txt
          venv/bin/python -m pip install pyinstaller

      - name: Check dependencies (fix)
        run: venv/bin/python check_deps.py --fix

      - name: Final dependency check (no fix)
        run: venv/bin/python check_deps.py

      - name: Convert .ico icon to .icns
        run: |
          venv/bin/python -c "
          from PIL import Image
          import subprocess, os
          img = Image.open('assets/logo-nom-stellio.ico')
          os.makedirs('iconset.iconset', exist_ok=True)
          for size in (16, 32, 64, 128, 256, 512, 1024):
              img.resize((size, size)).save(f'iconset.iconset/icon_{size}x{size}.png')
          subprocess.run(['iconutil', '-c', 'icns', 'iconset.iconset', '-o', 'assets/logo-nom-stellio.icns'], check=True)
          "

      - name: Build with PyInstaller
        run: |
          venv/bin/python -m PyInstaller \
            --name "Stellio" \
            --onedir \
            --windowed \
            --noconfirm \
            --clean \
            --hidden-import sqlite3 \
            --hidden-import ssl \
            --hidden-import hashlib \
            --hidden-import tkinter \
            --hidden-import tkinter.ttk \
            --hidden-import nest_asyncio \
            --collect-all tkinter \
            --collect-all webview \
            --collect-all trimesh \
            --collect-all pyrender \
            --collect-all imageio \
            --collect-all OpenGL \
            --collect-all numpy \
            --collect-all PIL \
            --collect-all matplotlib \
            --collect-all mpl_toolkits \
            --collect-all fast_simplification \
            --collect-all pymeshfix \
            --collect-all shapely \
            --collect-all rectpack \
            --collect-all smbprotocol \
            --collect-all smbclient \
            --collect-all paho \
            --collect-all cryptography \
            --collect-all rarfile \
            --collect-all py7zr \
            --collect-all defusedxml \
            --collect-all qrcode \
            --collect-all websocket \
            --collect-all flashforge \
            --collect-all waitress \
            --collect-all flask \
            --collect-all werkzeug \
            --copy-metadata imageio \
            --copy-metadata pyrender \
            --copy-metadata trimesh \
            --copy-metadata Pillow \
            --copy-metadata PyOpenGL \
            --copy-metadata numpy \
            --icon="assets/logo-nom-stellio.icns" "launcher.py"

      - name: Verify PyInstaller output exists
        run: |
          if [ ! -d "dist/Stellio.app" ]; then
            echo "dist/Stellio.app introuvable - la compilation a echoue"
            exit 1
          fi

      - name: Copy patchable files into Stellio.app/Contents/Resources/app
        run: |
          APP_DIR="dist/Stellio.app/Contents/Resources/app"
          mkdir -p "$APP_DIR"
          cp main.py "$APP_DIR/main.py"
          cp check_deps.py "$APP_DIR/check_deps.py"
          cp index.html "$APP_DIR/index.html"
          cp script.js "$APP_DIR/script.js"
          cp style.css "$APP_DIR/style.css"
          cp -R assets "$APP_DIR/assets"
          cp -R languages "$APP_DIR/languages"
          [ -d bin ] && cp -R bin "$APP_DIR/bin" || true
          for f in main.py check_deps.py index.html script.js style.css; do
            [ -f "$APP_DIR/$f" ] || { echo "Echec de copie : $APP_DIR/$f"; exit 1; }
          done

      - name: Code sign (Developer ID if available, else ad-hoc)
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PWD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
          MACOS_SIGNING_IDENTITY: ${{ secrets.MACOS_SIGNING_IDENTITY }}
        run: |
          if [ -n "$MACOS_CERTIFICATE" ]; then
            echo "$MACOS_CERTIFICATE" | base64 --decode > certificate.p12
            security create-keychain -p actions build.keychain
            security default-keychain -s build.keychain
            security unlock-keychain -p actions build.keychain
            security import certificate.p12 -k build.keychain -P "$MACOS_CERTIFICATE_PWD" -T /usr/bin/codesign
            security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k actions build.keychain
            codesign --deep --force --options runtime --sign "$MACOS_SIGNING_IDENTITY" "dist/Stellio.app"
            echo "Signe avec le certificat Developer ID."
          else
            codesign --deep --force --sign - "dist/Stellio.app"
            echo "Aucun certificat Developer ID configure — signature ad-hoc uniquement (secrets MACOS_CERTIFICATE / MACOS_CERTIFICATE_PWD / MACOS_SIGNING_IDENTITY absents)."
          fi

      - name: Create .dmg
        run: |
          mkdir -p dmg-root
          cp -R "dist/Stellio.app" dmg-root/
          ln -s /Applications dmg-root/Applications
          hdiutil create -volname "Stellio" -srcfolder dmg-root -ov -format UDZO \
            "Stellio-v${{ needs.check-version.outputs.version }}-macOS-${{ matrix.arch }}.dmg"
          shasum -a 256 "Stellio-v${{ needs.check-version.outputs.version }}-macOS-${{ matrix.arch }}.dmg" \
            > "Stellio-v${{ needs.check-version.outputs.version }}-macOS-${{ matrix.arch }}.dmg.sha256"

      - name: Upload macOS installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: stellio-macos-dmg-${{ matrix.arch }}
          path: |
            Stellio-v${{ needs.check-version.outputs.version }}-macOS-${{ matrix.arch }}.dmg
            Stellio-v${{ needs.check-version.outputs.version }}-macOS-${{ matrix.arch }}.dmg.sha256

  # ──────────────────────────────────────────────────────────────────────
  # 2ter. Build Linux (AppImage)
  # ──────────────────────────────────────────────────────────────────────
  build-linux:
    needs: check-version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies (GTK/WebKit pour pywebview + OSMesa pour le rendu 3D)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev libwebkit2gtk-4.1-dev gir1.2-webkit2-4.1 \
            python3-gi python3-gi-cairo gobject-introspection \
            libgirepository1.0-dev libgirepository-2.0-dev libcairo2-dev pkg-config python3-dev \
            libosmesa6-dev libgl1-mesa-dev \
            patchelf libfuse2

      - name: Install dependencies
        run: |
          python3 -m venv venv
          venv/bin/python -m pip install --upgrade pip
          venv/bin/python -m pip install --upgrade -r requirements.txt
          venv/bin/python -m pip install --upgrade "PyGObject>=3.52" pycairo
          venv/bin/python -m pip install pyinstaller

      - name: Check dependencies (fix)
        run: venv/bin/python check_deps.py --fix

      - name: Final dependency check (no fix)
        run: venv/bin/python check_deps.py

      - name: Build with PyInstaller
        run: |
          venv/bin/python -m PyInstaller \
            --name "Stellio" \
            --onedir \
            --noconfirm \
            --clean \
            --hidden-import sqlite3 \
            --hidden-import ssl \
            --hidden-import hashlib \
            --hidden-import tkinter \
            --hidden-import tkinter.ttk \
            --hidden-import nest_asyncio \
            --collect-all tkinter \
            --collect-all webview \
            --collect-all trimesh \
            --collect-all pyrender \
            --collect-all imageio \
            --collect-all OpenGL \
            --collect-all numpy \
            --collect-all PIL \
            --collect-all matplotlib \
            --collect-all mpl_toolkits \
            --collect-all fast_simplification \
            --collect-all pymeshfix \
            --collect-all shapely \
            --collect-all rectpack \
            --collect-all smbprotocol \
            --collect-all smbclient \
            --collect-all paho \
            --collect-all cryptography \
            --collect-all rarfile \
            --collect-all py7zr \
            --collect-all defusedxml \
            --collect-all qrcode \
            --collect-all websocket \
            --collect-all flashforge \
            --collect-all waitress \
            --collect-all flask \
            --collect-all werkzeug \
            --copy-metadata imageio \
            --copy-metadata pyrender \
            --copy-metadata trimesh \
            --copy-metadata Pillow \
            --copy-metadata PyOpenGL \
            --copy-metadata numpy \
            "launcher.py"

      - name: Verify PyInstaller output exists
        run: |
          if [ ! -f "dist/Stellio/Stellio" ]; then
            echo "dist/Stellio/Stellio introuvable - la compilation a echoue"
            exit 1
          fi

      - name: Copy patchable files into dist/Stellio/app
        run: |
          APP_DIR="dist/Stellio/app"
          mkdir -p "$APP_DIR"
          cp main.py "$APP_DIR/main.py"
          cp check_deps.py "$APP_DIR/check_deps.py"
          cp index.html "$APP_DIR/index.html"
          cp script.js "$APP_DIR/script.js"
          cp style.css "$APP_DIR/style.css"
          cp -R assets "$APP_DIR/assets"
          cp -R languages "$APP_DIR/languages"
          [ -d bin ] && cp -R bin "$APP_DIR/bin" || true
          for f in main.py check_deps.py index.html script.js style.css; do
            [ -f "$APP_DIR/$f" ] || { echo "Echec de copie : $APP_DIR/$f"; exit 1; }
          done

      - name: Assemble AppDir
        run: |
          mkdir -p AppDir/usr/bin AppDir/usr/share/icons/hicolor/256x256/apps AppDir/usr/share/applications
          cp -R dist/Stellio/* AppDir/usr/bin/
          venv/bin/python -c "
          from PIL import Image
          Image.open('assets/logo-nom-stellio.ico').resize((256, 256)).save('AppDir/usr/share/icons/hicolor/256x256/apps/stellio.png')
          "
          cp AppDir/usr/share/icons/hicolor/256x256/apps/stellio.png AppDir/stellio.png
          cat > AppDir/stellio.desktop <<'EOF'
          [Desktop Entry]
          Type=Application
          Name=Stellio
          Exec=Stellio
          Icon=stellio
          Categories=Utility;
          EOF
          cp AppDir/stellio.desktop AppDir/usr/share/applications/stellio.desktop

      - name: Download appimagetool and build the AppImage
        run: |
          wget -q -O appimagetool "https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage"
          chmod +x appimagetool
          ./appimagetool --appimage-extract-and-run AppDir "Stellio-v${{ needs.check-version.outputs.version }}-linux-x86_64.AppImage"
          chmod +x "Stellio-v${{ needs.check-version.outputs.version }}-linux-x86_64.AppImage"
          sha256sum "Stellio-v${{ needs.check-version.outputs.version }}-linux-x86_64.AppImage" \
            > "Stellio-v${{ needs.check-version.outputs.version }}-linux-x86_64.AppImage.sha256"

      - name: Upload Linux AppImage artifact
        uses: actions/upload-artifact@v4
        with:
          name: stellio-linux-appimage
          path: |
            Stellio-v${{ needs.check-version.outputs.version }}-linux-x86_64.AppImage
            Stellio-v${{ needs.check-version.outputs.version }}-linux-x86_64.AppImage.sha256

  # ──────────────────────────────────────────────────────────────────────
  # 3. Publication en DRAFT (Brouillon) pour édition manuelle
  # ──────────────────────────────────────────────────────────────────────
  release:
    needs: [check-version, build, build-macos, build-linux]
    if: needs.check-version.outputs.should_release == 'true'
    runs-on: windows-latest
    steps:
      - name: Download Windows installer artifact
        uses: actions/download-artifact@v4
        with:
          name: stellio-installer-unsigned
          path: release-files

      - name: Download patch ZIP artifact (commun aux 3 plateformes)
        uses: actions/download-artifact@v4
        with:
          name: stellio-patch-zip
          path: release-files

      - name: Download macOS .dmg artifacts (arm64 + x86_64)
        uses: actions/download-artifact@v4
        with:
          pattern: stellio-macos-dmg-*
          path: release-files
          merge-multiple: true

      - name: Download Linux AppImage artifact
        uses: actions/download-artifact@v4
        with:
          name: stellio-linux-appimage
          path: release-files

      - name: Compute SHA256 checksum of Windows installer
        shell: pwsh
        run: |
          $file = Get-ChildItem "release-files\*.exe" | Select-Object -First 1
          $hash = (Get-FileHash -Algorithm SHA256 $file.FullName).Hash.ToLower()
          "$hash  $($file.Name)" | Out-File -Encoding ascii "release-files\$($file.Name).sha256"

      - name: Create GitHub Release (Draft)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.check-version.outputs.version }}
          name: Stellio v${{ needs.check-version.outputs.version }}
          draft: true                     
          generate_release_notes: false   
          files: |
            release-files/*.exe
            release-files/*.dmg
            release-files/*.AppImage
            release-files/*.sha256
            release-files/*.zip

  # ──────────────────────────────────────────────────────────────────────
  # 4. Signature + publication AVEC SignPath (DÉSACTIVÉ pour l'instant)
  # 
  # POUR RÉACTIVER LA SIGNATURE :
  # 1. Supprime ou commente le job "release" ci-dessus
  # 2. Décommente toutes les lignes ci-dessous (retire les # au début)
  # 3. Configure les secrets GitHub suivants dans Settings > Secrets and variables > Actions :
  #    - SIGNPATH_API_TOKEN
  #    - SIGNPATH_ORG_ID
  #    - SIGNPATH_PROJECT_SLUG
  #    - SIGNPATH_POLICY_SLUG
  # ──────────────────────────────────────────────────────────────────────
  # sign-and-release:
  #   needs: [check-version, build]
  #   if: needs.check-version.outputs.should_release == 'true'
  #   runs-on: windows-latest
  #   steps:
  #     - name: Submit signing request to SignPath (installeur uniquement)
  #       uses: signpath/github-action-submit-signing-request@v2
  #       with:
  #         api-token: '${{ secrets.SIGNPATH_API_TOKEN }}'
  #         organization-id: '${{ secrets.SIGNPATH_ORG_ID }}'
  #         project-slug: '${{ secrets.SIGNPATH_PROJECT_SLUG }}'
  #         signing-policy-slug: '${{ secrets.SIGNPATH_POLICY_SLUG }}'
  #         github-artifact-id: '${{ needs.build.outputs.artifact-id }}'
  #         wait-for-completion: true
  #         output-artifact-directory: 'signed'
  #
  #     - name: Compute SHA256 checksum of signed installer
  #       shell: pwsh
  #       run: |
  #         $file = Get-ChildItem "signed\*.exe" | Select-Object -First 1
  #         $hash = (Get-FileHash -Algorithm SHA256 $file.FullName).Hash.ToLower()
  #         "$hash  $($file.Name)" | Out-File -Encoding ascii "signed\$($file.Name).sha256"
  #
  #     - name: Download patch ZIP artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: stellio-patch-zip
  #         path: patch
  #
  #     - name: Create GitHub Release (Draft)
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         tag_name: v${{ needs.check-version.outputs.version }}
  #         name: Stellio v${{ needs.check-version.outputs.version }}
  #         draft: true                      # ← Ajouté pour cohérence future
  #         generate_release_notes: false    # ← Ajouté pour cohérence future
  #         files: |
  #           signed/*.exe
  #           signed/*.sha256
  #           patch/*.zip
  #           patch/*.sha256